{
  "name": "rende-ia-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Testes end-to-end do frontend RENDE-IA com Playwright (backend mockado via route interception).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
